/* 
This function is stringified so everything is uses has to be defined inside the function
*/
const callBackendFunc = (funcName) => (param) => {
  console.log("test");
};

module.exports = callBackendFunc;
